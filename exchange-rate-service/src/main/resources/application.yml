server:
  port: 8080

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic

bank-service:
  exchangeApiClient:
    url: "https://api.exchangeratesapi.io"
  noSuchCurrencyErrorDescription: "Указанная валюта некорректна: "